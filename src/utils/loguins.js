// const log = require('log');

// const emailSchema = log.object({
//   email: log.string().email().required(),
//   password: log.string().min(6).required(),
// });

// module.exports = {
//   emailSchema,
// };
